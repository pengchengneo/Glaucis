# Auto-generated Glaucis config for L1 problem 4: Matrix_vector_multiplication_
kernel:
  name: "bench_004"
  template: "kernels/bench_004.py"
  reference: "kernels/bench_004_ref.py"
  evolve_markers:
    start: "# EVOLVE-BLOCK-START"
    end: "# EVOLVE-BLOCK-END"

shapes:
  - { _id: 4 }

correctness:
  method: "allclose"
  rtol: 1e-4
  atol: 1e-4

evaluator:
  namespace: "default"
  job_template: ".github/ci/kernel-eval-job.yaml"
  repo: "sii-xinglong/Glaucis"
  branch: "main"
  poll_interval: 15
  timeout: 600

tpu:
  cluster: "tpu7x-cluster"
  zone: "us-central1"

batch:
  variants_per_round: 3
  top_k: 1
  max_active_lineages: 2

session:
  max_iterations: 5
  output_dir: "runs/bench_004"
