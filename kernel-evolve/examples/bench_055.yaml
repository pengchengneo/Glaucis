# Auto-generated Glaucis config for L1 problem 55: conv_standard_2D__asymmetric_input__square_kernel
kernel:
  name: "bench_055"
  template: "kernels/bench_055.py"
  reference: "kernels/bench_055_ref.py"
  evolve_markers:
    start: "# EVOLVE-BLOCK-START"
    end: "# EVOLVE-BLOCK-END"

shapes:
  - { _id: 55 }

correctness:
  method: "allclose"
  rtol: 1e-2
  atol: 1e-2

evaluator:
  namespace: "default"
  job_template: ".github/ci/kernel-eval-job.yaml"
  repo: "sii-xinglong/Glaucis"
  branch: "main"
  poll_interval: 15
  timeout: 600

tpu:
  cluster: "tpu7x-cluster"
  zone: "us-central1"

batch:
  variants_per_round: 3
  top_k: 1
  max_active_lineages: 2

session:
  max_iterations: 5
  output_dir: "runs/bench_055"
